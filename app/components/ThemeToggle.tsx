'use client';
import { useEffect, useState } from 'react';
export default function ThemeToggle(){const[dark,setDark]=useState(false);useEffect(()=>{const saved=localStorage.getItem('relaybit-theme');const next=saved?saved==='dark':matchMedia('(prefers-color-scheme: dark)').matches;document.documentElement.dataset.theme=next?'dark':'light';const timer=window.setTimeout(()=>setDark(next),0);return()=>window.clearTimeout(timer)},[]);function toggle(){const next=!dark;setDark(next);document.documentElement.dataset.theme=next?'dark':'light';localStorage.setItem('relaybit-theme',next?'dark':'light')}return <button className="theme-toggle" onClick={toggle} aria-label={dark?'Включить светлую тему':'Включить тёмную тему'}><span>{dark?'☀':'◐'}</span><b>{dark?'Светлая':'Тёмная'}</b></button>}
